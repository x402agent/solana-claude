{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        // @workaround DTS bug
        // dts do not work with monorepo path mapping
        // disable path mapping for it
        "paths": {}
    }
}
