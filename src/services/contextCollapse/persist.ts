/**
 * Context collapse persistence — stub.
 * Provides a no-op implementation for restoring context collapse state.
 */

export function restoreFromEntries(
  _commits: unknown[],
  _snapshot?: unknown,
): void {
  // no-op stub
}
