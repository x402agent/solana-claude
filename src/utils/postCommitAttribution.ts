/**
 * Post-commit attribution hook — stub.
 * Provides a no-op implementation for the COMMIT_ATTRIBUTION feature gate.
 */

export async function installPrepareCommitMsgHook(
  _worktreePath: string,
  _worktreeHooksDir?: string,
): Promise<void> {
  // no-op stub
}
