{
  "name": "markdown-plugin-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev --listen 3400",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@solana-clawd/plugin-sdk": "^1.0.0",
    "@solana-clawd/chat-plugins-gateway": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vercel": "^33.0.0"
  }
}

